{
    IBClasses = (
        {
            ACTIONS = {invokeAction = id; }; 
            CLASS = AMPurpleSearchResultsController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                buttonview = NSView; 
                tableview = NSTableView; 
                "textfield_primary" = NSTextField; 
                "textfield_secondary" = NSTextField; 
                window = NSWindow; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}