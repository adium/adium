{
    IBClasses = (
        {
            ACTIONS = {showPrefs = id; }; 
            CLASS = AXCPreferenceController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                prefsWindow = NSWindow; 
                startupActionPopup = NSPopUpButton; 
                userDefaults = NSUserDefaultsController; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}