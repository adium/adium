{
    IBClasses = (
        {CLASS = AITextColorPreviewView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {
            CLASS = AdiumIBPalette; 
            LANGUAGE = ObjC; 
            OUTLETS = {previewView = AITextColorPreviewView; }; 
            SUPERCLASS = IBPalette; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}